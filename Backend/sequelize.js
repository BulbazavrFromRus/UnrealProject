/*const { Sequelize } = require('sequelize');
const config = require('./config.json'); // Подключение к файлу конфигурации

const sequelize = new Sequelize(config.development);

module.exports = sequelize;*/
